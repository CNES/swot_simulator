version: 2

sphinx:
  builder: html
  configuration: docs/source/conf.py

python:
  version: 3.7
  install:
    - path: .
      method: setuptools

conda:
  environment: conda/environment.yml

build:
  image: latest
